export const MAX_WIDTH_THUMBNAIL = Math.round(250 * 1.5);
export const MAX_HEIGHT_THUMBNAIL = Math.round(130 * 1.5);

// Were drawing id & title is saved in local storage
export const DRAWING_ID_KEY_LS = "__drawing_id";
export const DRAWING_TITLE_KEY_LS = "__drawing_title";
